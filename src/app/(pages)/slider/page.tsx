import DonutSectionSlider from "@/app/components/DonutSectionSlider";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Slider ",
};

export default function page() {
  return <DonutSectionSlider />;
}
